import create from './lib/state.blop'
import { default as fetch } from 'node-fetch'

state = {
  todoList: [
    'some task 1',
    'some task 2'
  ],
  dog: null,
  loading: false
}

def createState() {
  return create(state)
}
