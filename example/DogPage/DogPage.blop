import { DogChooser } from './DogChooser.blop'

def DogPage(attributes) {
  { state } = attributes
  <div>
    <DogChooser dog=state.dog state=state
      loading=state.loading>state.choosenDoggy || 'Dog choser'</DogChooser>
  </div>
}