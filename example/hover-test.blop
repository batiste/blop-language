type User = {
    name: string,
    id: number,
    userType?: "Admin" | "User"
}

type Profile = {
    user: User,
    bio?: string,
    verified: boolean
}

def profile(prof: Profile) {
    return prof.user
}

userAssignmentTest: User = { id: 1, name: "Alice" }
console.log(userAssignmentTest)

hello = "Hello"
hello := "asdf"
console.log(userAssignmentTest)