
a = 1
b = 2

while true {
  a := 2
  break
  b := 3
  continue
}

c = 0
myobject = {}
for key, value in [1, 10, 100, 1000]: array {
  if value > 99 {
    myobject['m'key''] = value
    // break
  }
  c := key
}

test('loops', () => {
  expect(a).toBe(2)
  expect(b).toBe(2)
  expect(c).toBe(3)
  expect(myobject).toEqual({ m2: 100, m3: 1000 })
})
