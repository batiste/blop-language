
type User = {
    name: string,
    id: number,
    bigAss: boolean
}

type Post = {
    title: string,
    author: string,
    views: number
}

type Profile = {
    user: User,
    bio?: string,
    verified: boolean
}