
type User = {
    name: string,
    id: number,
}

type Post = {
    title: string,
    author: string,
    views: number
}

type Profile = {
    user: User,
    bio?: string,
    verified: boolean
}

type choices = "option1" | "option2" | "option3"